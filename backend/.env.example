# IdentityShield Backend Environment Variables

# ============ LLM Configuration ============
# Priority: Gemini (if key set) > Ollama (if enabled) > Claude

# Option 1: Google Gemini (free tier available)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# Option 2: Ollama (local, free)
# USE_OLLAMA=true
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# Option 3: Claude (paid API)
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# ============ Storage ============
DATABASE_PATH=./data/identityshield.db
EMBEDDING_STORAGE_PATH=./data/embeddings
